{
  "name": "@gander-tools/visual-regression-testing-system",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0-only",
  "private": true,
  "scripts": {
    "visual:generate": "node scripts/generate-visual-baseline.mjs",
    "visual:test": "node scripts/run-visual-tests.mjs",
    "visual:report": "playwright show-report .visual-regression/report",
    "visual:inspect": "node scripts/inspect-page.mjs"
  }
}
